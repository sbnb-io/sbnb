---
# Example inventory file for SBNB compute collection
#
# Customize this file for your environment

all:
  children:
    # GPU-enabled hosts for ML workloads
    gpu_hosts:
      hosts:
        # worker-gpu-01:
        #   ansible_host: 192.168.1.101
        # worker-gpu-02:
        #   ansible_host: 192.168.1.102

    # CPU-only hosts for general workloads
    cpu_hosts:
      hosts:
        # worker-cpu-01:
        #   ansible_host: 192.168.1.111

    # Development/testing hosts
    dev:
      hosts:
        # dev-machine:
        #   ansible_host: 192.168.1.200

    # Confidential computing hosts (with AMD SEV-SNP support)
    secure_hosts:
      hosts:
        # secure-worker-01:
        #   ansible_host: 192.168.1.150
