#!/usr/bin/env python3
"""Data-plane executable: serves the io.reefy.Reconciler Varlink
interface and does the storage/container work, no MQTT. Runs only the
data-plane entrypoint - this file IS the data-plane role, no flags, no
role-branching."""

import sys

sys.path.insert(0, '/usr/lib/reefy')

from reefy.dataplane import main_data_plane

main_data_plane()
