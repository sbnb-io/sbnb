#!/usr/bin/env python3
"""Boot-mount executable: pre-docker oneshot that mounts per-app
volumes from persisted desired-state, then exits. Runs only the
boot-mount entrypoint - this file IS the boot-mount role, no flags, no
role-branching."""

import sys

sys.path.insert(0, '/usr/lib/reefy')

from reefy.storage import main_boot_mount

main_boot_mount()
